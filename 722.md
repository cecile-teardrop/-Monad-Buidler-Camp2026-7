# Moss 入门指南：从零到 AI Agent 驱动的链上交易

>  教程方向 ：Moss 入门指南 + AI Agent Demo 搭建
>  适合人群 ：对 Web3 / AI Agent / Monad 生态感兴趣的开发者
>  预计学习时间 ：2-3 小时（含环境搭建）
>  前置知识 ：基本的命令行操作；了解 TypeScript 基础语法更佳，但非必需
>  教程版本 ：基于 Moss 主分支（2026 年 7 月）

---

## 目录

- [第一章 理解 Moss：它是什么，为什么存在](#第一章-理解-moss它是什么为什么存在)
- [第二章 环境准备：从零搭建开发环境](#第二章-环境准备从零搭建开发环境)
- [第三章 第一次运行：跑通完整的 Wrap 流程](#第三章-第一次运行跑通完整的-wrap-流程)
- [第四章 四步流程详解：discover → load → action → simulate](#第四章-四步流程详解discover--load--action--simulate)
- [第五章 用代码组装 Moss：从零构建一个 Swap](#第五章-用代码组装-moss从零构建一个-swap)
- [第六章 连接 AI Agent：通过 MCP 驱动 Moss](#第六章-连接-ai-agent通过-mcp-驱动-moss)
- [第七章 搭建本地 Agent Swap Demo](#第七章-搭建本地-agent-swap-demo)
- [第八章 常见问题 FAQ](#第八章-常见问题-faq)
- [第九章 下一步：参与开源贡献](#第九章-下一步参与开源贡献)

---

## 第一章 理解 Moss：它是什么，为什么存在

   1.1 一句话定义

 Moss 是一个把 Monad 区块链上的协议交互转化为 AI Agent 可调用能力的框架。 

它的工作流程是四个步骤：`discover → load → action → simulate`（发现协议 → 加载能力 → 构建交易 → 模拟验证）。

   1.2 Moss 解决什么问题

想象你让 AI Agent 帮你做一笔 DeFi 交易——"帮我把 1 个 MON 换成 USDC"。

 没有 Moss 的世界 ：Agent 需要自己理解 Kuru DEX 的合约接口、手动拼装 calldata、估算滑点、计算授权额度。这些操作直接涉及真实资金的签名和发送，风险极高。

 有 Moss 的世界 ：Agent 只需要调用四个标准化的工具——发现有什么操作可用、了解参数含义、构建交易、模拟验证结果。 Moss 构建和验证未签名的交易，但从不签名，也从不发送。  签名权始终掌握在用户自己的钱包里。

```
┌──────────────┐                  ┌──────────────┐
│   AI Agent    │                  │    Moss      │
│  (MCP 客户端) │ ──── 四步调用 ──→ │ (构建+验证)   │
│              │ ←── 结构化结果 ── │              │
└──────────────┘                  └──────┬───────┘
                                         │
                                  未签名的交易
                                  + 验证回执
                                         │
                                         ▼
                                 ┌──────────────┐
                                 │   用户钱包     │
                                 │ (签名+发送)    │
                                 └──────────────┘
```

   1.3 三大设计原则

| 原则 | 含义 | 对你的意义 |
| --- | --- | --- |
|  协议拥有操作  | Protocol 包拥有合约地址、ABI、calldata 构建逻辑 | 你不需要手动查合约地址或拼 ABI |
|  模拟产生证据  | 每笔成功交易产生不可变的 Change 列表和结构化 Receipt | 你能看到交易到底做了什么，而不只是"成功"或"失败" |
|  签名保持独立  | Moss 只构建和验证，签名和发送由独立的钱包完成 | 你的私钥永远不暴露给 Agent 或 Moss |

   1.4 Moss 支持哪些协议

截至本教程编写时，Moss 支持以下协议：

| 协议 | 包名 | 能力（Capability） | 查询（Query） |
| --- | --- | --- | --- |
|  WMON （Wrapped Monad） | `@themoss/system` | `wrap`、`unwrap` | `balanceOf` |
|  ERC-20 代币  | `@themoss/erc` | `transfer`、`approve` | `balanceOf`、`allowance`、`metadata` |
|  ERC-721 NFT  | `@themoss/erc` | `transfer` | `ownerOf`、`balanceOf` |
|  Kuru DEX  | `@themoss/protocol-kuru` | `swap` | `quote` |
|  PancakeSwap V3  | `@themoss/protocol-pancakeswap` | 单跳 swap | quote |

   1.5 重要提醒

> ⚠️  Moss 目前是未经审计的 Alpha 软件，不应与生产资金配合使用。 
>
> 所有示例都在 Monad 主网状态上模拟，但 不会签名、不会发送任何交易 ，因此不需要私钥或有资金的账户。

---

## 第二章 环境准备：从零搭建开发环境

   2.1 你需要安装什么

| 工具 | 版本要求 | 安装方式 | 说明 |
| --- | --- | --- | --- |
|  Node.js  | 22 或更新 | [nodejs.org](https://nodejs.org/) 下载 | 运行 TypeScript 代码的基础 |
|  pnpm  | 11 或更新 | `npm install -g pnpm` | Moss 的包管理器 |
|  Git  | 任意版本 | [git-scm.com](https://git-scm.com/) | 克隆仓库 |

 可选 （如果你想跑第七章的本地 Agent Swap Demo）：

| 工具 | 说明 |
| --- | --- |
|  Foundry（Monad 版）  | 提供 `anvil` 本地 fork 节点，用于本地模拟 |
|  Claude Code  | 如果你想通过 AI Agent 驱动 Moss |

   2.2 验证环境

打开终端，依次执行：

```bash
node --version    # 应输出 v22.x.x 或更高
pnpm --version    # 应输出 11.x.x 或更高
git --version     # 应输出 git version 2.x
```

如果任何一条命令报错"command not found"，请先安装对应工具。

   2.3 克隆并构建 Moss

```bash
# 1. 克隆仓库
git clone https://github.com/nishuzumi/moss
cd moss

# 2. 安装依赖
pnpm install

# 3. 构建所有包
pnpm build
```

 构建说明 ：Moss 是一个 pnpm monorepo，包含多个相互依赖的包。`pnpm build` 会按依赖顺序构建所有包，生成的类型声明文件（`dist/`）是后续 typecheck 的前提， 必须先 build 再做其他操作 。

   2.4 验证安装：跑通离线测试

```bash
# 跑离线测试（不需要网络连接和 RPC）
pnpm test:offline
```

如果所有测试通过，恭喜你——环境搭建成功！🎉

   2.5 配置 RPC（连接 Monad 主网）

Moss 的示例需要读取 Monad 主网的实时状态（来构建真实的交易），因此需要一个 RPC 端点。

 方式一：使用默认公共 RPC 

Moss 默认使用 `https://rpc.monad.xyz`，你不需要做任何配置。

 方式二：使用自定义 RPC 

如果你有自己的 RPC 端点，可以通过环境变量指定：

```bash
# Linux / macOS
export MOSS_RPC_URL="https://your-rpc-endpoint.com"

# Windows (PowerShell)
$env:MOSS_RPC_URL = "https://your-rpc-endpoint.com"

# Windows (Git Bash)
export MOSS_RPC_URL="https://your-rpc-endpoint.com"
```

 RPC 要求 ：端点必须支持 `debug_traceCall`（含 call/log 证据）、`prestateTracer` diff 和 state overrides。默认的 `https://rpc.monad.xyz` 满足这些要求。如果端点不支持，Moss 会返回 Warning 并停止， 绝不会降级到近似排序 。

---

## 第三章 第一次运行：跑通完整的 Wrap 流程

   3.1 什么是 Wrap

在 Monad 网络中，原生代币是  MON 。但很多 DeFi 协议需要使用  WMON （Wrapped MON）——一种符合 ERC-20 标准的代币形式。"Wrap"就是把 MON 换成 WMON 的操作。

   3.2 运行第一个示例

```bash
pnpm --filter @themoss/example-simple-flow wrap
```

 这条命令做了什么 ：

1.  发现  WMON 协议的 `wrap` 能力
2.  加载  wrap 的参数契约
3.  构建  一个 wrap 的 Capability（未签名交易）
4.  模拟  这笔交易并解析 Receipt

   3.3 理解输出

运行后你会看到类似这样的输出：

```
✓ Simulation passed with 0 warnings

Receipt:
  operation: wrap
  protocol: system
  ...
  outcome:
    amountIn: 1000000000000000000  (1 MON)
    amountOut: 1000000000000000000 (1 WMON)
```

 最重要的验证点 不是"成功"两个字，而是：
- ✅  零 Warning （`warnings: []`）
- ✅  结构化 Outcome 与请求匹配 （amountIn = 1 MON，amountOut = 1 WMON）

   3.4 运行 Swap 示例

```bash
pnpm --filter @themoss/example-simple-flow swap
```

这个示例更复杂——它在 Kuru DEX 上把 1 MON 换成 USDC。你会看到：
- 先获取报价（Quote）
- 再构建 swap Capability
- 最后模拟验证

> 💡  这些示例读取的是 Monad 主网的实时状态，但不需要私钥或资金 ——因为 Moss 只构建和模拟未签名交易，从不发送。

---

## 第四章 四步流程详解：discover → load → action → simulate

这是 Moss 的核心工作流程，理解它就理解了 Moss。

   4.1 流程总览

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ discover │───▶│   load   │───▶│  action  │───▶│ simulate │
│ 发现协议  │    │ 加载能力  │    │ 构建交易  │    │ 模拟验证  │
└──────────┘    └──────────┘    └──────────┘    └────┬─────┘
                                                     │
                                               ┌─────▼─────┐
                                               │ 模拟通过？ │
                                               └─────┬─────┘
                                          YES ◀──────┼──────▶ NO
                                           │              │
                                  ┌────────▼────────┐    │
                                  │ 交给钱包签名发送  │    │
                                  │ (Moss 本身不签名) │    │
                                  └─────────────────┘    └─▶ 拒绝
```

   4.2 第一步：discover — 发现有什么操作可用

 作用 ：在加载完整参数之前，先快速查找有哪些 Capability 和 Query 可用。

```ts
// 查找所有 "swap" 操作
registry.discover({ verb: "swap" });
```

 输入参数 （至少提供一个）：

| 参数 | 类型 | 说明 | 例子 |
| --- | --- | --- | --- |
| `verb` | 字符串 | 用户操作动词 | `"swap"`、`"wrap"`、`"transfer"`、`"approve"` |
| `category` | 字符串 | 协议领域 | `"dex"`、`"token"`、`"nft"` |
| `protocol` | 字符串 | 精确协议名 | `"kuru"`、`"system"` |

 输出 ：一组坐标（Coordinate），每个坐标是一个 `{ protocol, method, kind, ... }` 对象。

```ts
// discover 的输出示例
[
  {
    protocol: "kuru",
    method: "swap",
    kind: "capability",
    verb: "swap",
    category: "dex",
    tags: ["clob", "orderbook"],
    summary: "Swap tokens on Kuru orderbook"
  },
  {
    protocol: "kuru",
    method: "quote",
    kind: "query",
    category: "dex",
    summary: "Get a quote for a swap"
  }
]
```

> ⚠️  重要 ：`discover` 只返回坐标和元数据， 不返回参数 schema 。你需要用 `load` 才能了解参数详情。 永远不要从 `discover` 的结果直接猜参数名去调用 `action` 。

   4.3 第二步：load — 了解参数详情

 作用 ：加载某个操作的完整参数契约——包括意图说明、风险标签、每个参数的类型和描述。

```ts
const [swap] = registry.load([{ protocol: "kuru", method: "swap" }]);
```

 输出 ：一个 Stub 对象，包含：

| 字段 | 说明 |
| --- | --- |
| `intent` | 这个操作做什么（自然语言描述） |
| `risk` | 风险标签列表（如滑点、授权） |
| `params` | 每个参数的 `type`（JSON Schema）和 `description`（语义说明） |

 关键理解 ：每个参数有 两份独立的描述 ：
- `type`：告诉你数据格式和单位（例如 `slippage` 的 type 会说明"basis points，50 = 0.5%"）
- `description`：告诉你这个字段控制什么（例如 `slippage` 的 description 会说明"此值限制输出减少的上限"）

> 💡  最佳实践 ： 永远在 `action` 之前调用 `load` 。不要从参数名猜测单位、默认值或地址。

   4.4 第三步：action — 构建交易或执行查询

 作用 ：根据参数构建一个 Capability（未签名交易树），或执行一个即时查询。

 Query（查询） ——立即返回结果，不产生 Capability：

```ts
const quote = await registry.action("kuru", "quote", ACCOUNT, {
  tokenIn: NATIVE,
  tokenOut: USDC_ADDRESS,
  amountIn: "1",
});
// quote.kind === "query"
// quote.data 包含当前市场报价
```

 Capability（能力） ——返回一棵未签名交易树：

```ts
const result = await registry.action("kuru", "swap", ACCOUNT, {
  tokenIn: NATIVE,
  tokenOut: USDC_ADDRESS,
  amountIn: "1",
  slippage: 50,  // 50 bps = 0.5%
});
// result.kind === "capability"
```

 Capability 树的结构 ：

```
Capability (kuru/swap)
├── TransactionNode (Kuru 合约调用)
│   ├── from: 你的地址
│   ├── to: Kuru 路由合约地址
│   ├── data: 编码后的 calldata
│   └── value: 附带的 MON 数量
```

如果是 ERC-20 代币的 swap，树会更深——会包含一个嵌套的 `approve` Capability：

```
Capability (kuru/swap)
├── Capability (erc20/approve)        ← 先授权
│   └── TransactionNode (approve 调用)
└── TransactionNode (Kuru swap 调用)   ← 再交易
```

> ⚠️  重要 ： 永远不要手动编辑或重新排列返回的 Capability 树 。如果参数变了，重新调用 `action`。

   4.5 第四步：simulate — 模拟验证

 作用 ：在 Monad 主网状态上模拟执行 Capability 树，解析每笔交易的 Receipt，并检查覆盖完整性。

```ts
const simulation = await simulator.simulate(capability);
```

 输出 ：

```ts
{
  halted: false,        // 是否中止
  results: [
    {
      protocol: "kuru",
      method: "swap",
      receipt: {
        outcome: {     // ← 结构化结果，这是证据
          operation: "swap",
          amountIn: "1",
          amountOut: "0.998...",  // 实际换到的 USDC 数量
        },
        text: "Kuru Swap: ..."  // ← 人类可读文本，仅用于展示
      },
      changes: [...],   // 不可变的原始事件列表
      warnings: [],     // ← 零 warning 才能继续
    }
  ]
}
```

 六种 Warning 代码 （任何一个出现都必须停止）：

| 代码 | 含义 |
| --- | --- |
| `REVERTED` | 交易回滚了 |
| `TRACE_FAILED` | RPC 无法提供追踪证据 |
| `CHANGE_ORDER_UNAVAILABLE` | 无法证明事件的精确顺序 |
| `RECEIPT_FAILED` | 协议无法把 Changes 解析为有效 Receipt |
| `CHANGE_COVERAGE_MISMATCH` | Receipt 遗漏、重复或重排了 Changes |
| `STATE_CHAIN_FAILED` | 后续交易的状态无法从前一笔推导 |

> ⚠️  铁律 ： 任何 Warning 都是终止性的 。出现 Warning 后，不要签名，不要发送，先排查原因。

   4.6 模拟通过 ≠ 交易正确

 零 Warning 只意味着"每个观察到的 Change 都被解析了" ——它不意味着结果符合你的意图。

最后一步必须做 意图对齐检查 ：

```ts
// 检查结构化 Outcome 是否与用户请求匹配
const outcome = simulation.results[0].receipt.outcome;
if (outcome.operation !== "swap") throw new Error("操作不匹配");
if (outcome.protocol !== "kuru") throw new Error("协议不匹配");
if (outcome.amountIn !== "1") throw new Error("输入金额不匹配");
// ... 检查 tokenIn、tokenOut、sender 等
```

> 💡  Receipt 的文本（text）是给人看的展示，不是证据。结构化的 Outcome 才是证据。永远不要因为文本中包含"Kuru Swap"字样就认为交易正确。 

---

## 第五章 用代码组装 Moss：从零构建一个 Swap

本章带你用 TypeScript 代码手动组装 Moss 的核心组件，理解每一步在做什么。

   5.1 创建脚本文件

在 `examples/simple-flow/src/` 目录下创建 `play.ts`：

```bash
# 在项目根目录执行
cp examples/simple-flow/src/wrap.ts examples/simple-flow/src/play.ts
```

   5.2 组装 Registry

打开 `play.ts`，把内容替换为：

```ts
import { NATIVE, Registry } from "@themoss/core";
import * as erc from "@themoss/erc";
import * as kuru from "@themoss/protocol-kuru";
import { createTraceSimulator } from "@themoss/simulator";
import * as system from "@themoss/system";
import { monadRuntime, USDC_ADDRESS } from "@themoss/system";

// 1. 定义发送者地址（这是一个示例地址，不是真实账户）
const ACCOUNT = "0xcccccccccccccccccccccccccccccccccccccccc";

// 2. 创建 Monad 运行时（连接主网读取状态）
const runtime = await monadRuntime({
  ...(process.env.MOSS_RPC_URL ? { rpcUrl: process.env.MOSS_RPC_URL } : {}),
});

// 3. 组装 Registry —— 这是 Moss 的核心组装点
//    你选择要加载哪些协议模块
const registry = new Registry(runtime).use(system, erc, kuru);

// 4. 创建模拟器
const simulator = createTraceSimulator(runtime, {
  receipt: (capability, changes) => registry.parseReceipt(capability, changes),
});

console.log("✓ Moss 组装完成");
```

 运行 ：

```bash
pnpm --filter @themoss/example-simple-flow exec tsx src/play.ts
```

   5.3 四步流程的完整代码

在 `play.ts` 末尾继续添加：

```ts
// ========== 第 0 步：记录意图 ==========
// "用 1 个 MON 在 Kuru 上换成 USDC，最多允许 0.5% 滑点"
const intent = {
  operation: "swap",
  tokenIn: NATIVE,          // 原生 MON
  tokenOut: USDC_ADDRESS,    // USDC 合约地址
  amountIn: "1",             // 人类可读的十进制字符串
  slippage: 50,              // 50 bps = 0.5%
  sender: ACCOUNT,
  protocol: "kuru",
};

// ========== 第 1 步：discover ==========
console.log("\n--- 第 1 步：discover ---");
const candidates = registry.discover({ verb: "swap" });
console.log("找到的 swap 操作：", candidates.length, "个");
candidates.forEach(c => console.log(`  - ${c.protocol}/${c.method} [${c.kind}]`));

// ========== 第 2 步：load ==========
console.log("\n--- 第 2 步：load ---");
const [swapStub] = registry.load([{ protocol: "kuru", method: "swap" }]);
console.log("意图：", swapStub.intent);
console.log("参数：");
for (const [name, param] of Object.entries(swapStub.params)) {
  console.log(`  ${name}: ${param.description}`);
}

// ========== 第 3 步：action ==========
console.log("\n--- 第 3 步：action ---");

// 先查一下报价（Query，立即返回）
const quote = await registry.action("kuru", "quote", ACCOUNT, {
  tokenIn: NATIVE,
  tokenOut: USDC_ADDRESS,
  amountIn: "1",
});
if (quote.kind !== "query") throw new Error("期望返回 Query 结果");
console.log("当前报价：", quote.data);

// 构建 swap Capability
const result = await registry.action("kuru", "swap", ACCOUNT, {
  tokenIn: NATIVE,
  tokenOut: USDC_ADDRESS,
  amountIn: "1",
  slippage: 50,
});
if (result.kind !== "capability") throw new Error("期望返回 Capability");
const capability = result;
console.log("✓ Capability 构建完成");

// ========== 第 4 步：simulate ==========
console.log("\n--- 第 4 步：simulate ---");
const simulation = await simulator.simulate(capability);

// 检查是否有 Warning
if (simulation.halted || simulation.results.some(r => r.warnings.length > 0)) {
  console.error("✗ 模拟发现 Warning，停止！");
  console.dir(simulation.results, { depth: null });
  throw new Error("模拟未通过");
}

// ========== 第 5 步：意图对齐检查 ==========
console.log("\n--- 第 5 步：意图对齐 ---");
for (const item of simulation.results) {
  const outcome = item.receipt?.outcome;
  console.log("结构化 Outcome：", outcome);

  // 逐一检查与意图的匹配
  // 注意：这里只是示例，实际检查会更详细
  console.log("Receipt 文本（仅展示）：", item.receipt?.text);
}

console.log("\n✓ 全流程完成！零 Warning，结构化 Outcome 已验证。");
console.log("  → 现在可以把 Capability 交给钱包签名发送（Moss 本身不签名）");
```

 运行完整脚本 ：

```bash
pnpm --filter @themoss/example-simple-flow exec tsx src/play.ts
```

你会看到四步流程的完整输出。这个脚本就是 Moss 工作流程的最小可运行实现。

   5.4 关键代码理解

| 代码 | 作用 |
| --- | --- |
| `new Registry(runtime)` | 创建核心注册表，从空开始 |
| `.use(system, erc, kuru)` | 显式组装要加载的协议模块 |
| `NATIVE` | 原生 MON 的标识符（不是地址，是语义常量） |
| `USDC_ADDRESS` | USDC 的官方合约地址（系统常量） |
| `amountIn: "1"` |  字符串 ，不是数字。链上量使用十进制字符串避免精度丢失 |
| `slippage: 50` | basis points，50 = 0.5%（不是百分比） |

---

## 第六章 连接 AI Agent：通过 MCP 驱动 Moss

   6.1 什么是 MCP

 MCP（Model Context Protocol）  是一个让 AI Agent 调用外部工具的标准协议。Moss 实现了 MCP Server，通过 stdio 暴露四个工具——`discover`、`load`、`action`、`simulate`——让任何 MCP 客户端（如 Claude）都能驱动 Moss。

   6.2 配置 MCP Server

首先确保你已经构建了 Moss：

```bash
pnpm build
```

然后在你的 MCP 客户端配置中添加 Moss Server。配置文件的位置取决于你使用的客户端：

 Claude Desktop （`claude_desktop_config.json`）：

```jsonc
{
  "mcpServers": {
    "moss": {
      "command": "node",
      "args": ["/你的路径/moss/packages/mcp-server/dist/cli.js"],
      "env": {
        "MOSS_RPC_URL": "https://rpc.monad.xyz"
      }
    }
  }
}
```

 Claude Code （项目根目录的 `.mcp.json`）：

```jsonc
{
  "mcpServers": {
    "moss": {
      "command": "node",
      "args": ["./packages/mcp-server/dist/cli.js"],
      "env": {
        "MOSS_RPC_URL": "https://rpc.monad.xyz"
      }
    }
  }
}
```

   6.3 四个 MCP 工具的输入输出

| 工具 | 输入 | 输出 |
| --- | --- | --- |
|  discover  | `{ verb?, category?, protocol? }` | 坐标列表 `[{protocol, method, kind, ...}]` |
|  load  | `{ items: [{protocol, method}] }` | 参数契约 `[{intent, risk, params, ...}]` |
|  action  | `{ protocol, method, account, params }` | Query 结果或 Capability 树 |
|  simulate  | `{ capability }` | 模拟结果 `{ ok, guidance, results, warnings }` |

   6.4 用自然语言驱动 Moss

配置好 MCP 后，你可以在 Claude 中直接用自然语言操作：

```
> 使用 moss 查一下 Kuru 上 1 MON 能换多少 USDC

Claude 会自动：
1. discover({ verb: "swap", protocol: "kuru" })  → 发现 swap 和 quote
2. load({ items: [{protocol: "kuru", method: "quote"}] })  → 了解参数
3. action({ protocol: "kuru", method: "quote", account: "0xcccc...", params: {...} })
4. 返回报价结果
```

   6.5 Agent 安全规则

Moss 的 MCP Server 遵循严格的安全规则。当 Agent 调用 `simulate` 后，它必须：

1.  检查 `ok` 字段 ——如果是 `false`，停止
2.  检查每个 result 的 `warnings` ——如果有任何 warning，停止
3.  把每个 Receipt 的 `texts` 与用户原始请求做意图对齐 ——不是只看文本中有没有"swap"字样，而是验证操作、资产、金额、接收者等结构化字段
4.  不修改 Capability 树 ——如果参数变了，重新调用 `action` 和 `simulate`

> ⚠️  铁律 ：Agent 必须在 `simulate` 通过后才把 Capability 交给钱包。 `simulate` 是签名前的最后一道闸门。 

---

## 第七章 搭建本地 Agent Swap Demo

这是本教程的高潮部分——搭建一个完整的端到端 Agent Swap Demo，在本地 Monad fork 上完成一笔真实的交易。

   7.1 这个 Demo 做什么

三个角色协作完成一笔交易：

```
┌────────────┐         ┌────────────┐         ┌────────────┐
│   Agent     │         │    Moss     │         │   Wallet    │
│ (Claude)   │────────→│ (构建+验证) │────────→│ (签名+发送) │
│            │  MCP    │            │  JSON   │            │
└────────────┘         └────────────┘         └────────────┘
     记录意图              构建未签名交易          只签验证过的交易
     驱动四步流程          模拟并检查 Warning      发送到本地 fork
```

 关键安全设计 ：Moss 永远不接收私钥。钱包是一个独立进程，只签验证过的 Capability 树。

   7.2 安装前置依赖

除了第二章的工具外，你还需要安装  Foundry（Monad 版） ，它提供 `anvil` 本地 fork 节点：

```bash
# 安装 Foundry
curl -L https://foundry.category.xyz | bash

# 安装 Monad 版本
foundryup --network monad
```

验证安装：

```bash
anvil --version  # 应能正常运行
```

   7.3 启动本地 Fork

```bash
# 在 Moss 项目根目录
pnpm --filter @themoss/example-agent-swap fork
```

这个命令会：
1. 启动一个本地 `anvil` 节点，fork Monad 主网的最新状态
2. 用 Anvil 的公开开发账户 #0 资金一些测试 MON

> ⚠️  安全提示 ：示例中使用的私钥是 Anvil 的公开开发账户。它只在本地 fork 上有价值。 永远不要在真实网络上使用这个密钥，也不要给它充值。 

   7.4 构建并验证交易

打开一个 新的终端 （保持 fork 终端运行）：

```bash
# 构建并模拟一笔 swap，输出到 JSON 文件
pnpm --filter @themoss/example-agent-swap swap -- verified-capability.json
```

 这个脚本做了什么 ：

1. 记录意图："用 1 MON 在 Kuru 上换 USDC，滑点 0.5%"
2. 调用 `discover → load → action → simulate`
3. 如果有任何 Warning →  停止 
4. 检查结构化 Outcome 与意图是否匹配
5. 把验证过的 Capability 写入 `verified-capability.json`

 检查输出 ：打开 `verified-capability.json`，你会看到一棵完整的未签名交易树。 不要手动编辑这个文件。 

   7.5 签名并发送（在本地 Fork 上）

```bash
# 用钱包签名并发送到本地 fork
pnpm --filter @themoss/example-agent-swap wallet -- send verified-capability.json
```

钱包会：
1. 验证 Capability 树的发送者是否匹配
2. 验证本地 RPC 的 chain ID 是 143（Monad）
3. 逐笔签名并发送

   7.6 通过 AI Agent 驱动

如果你想用 Claude Code 驱动整个流程：

1. 确保 fork 正在运行（`pnpm --filter @themoss/example-agent-swap fork`）
2. 在 Moss 项目根目录打开 Claude Code
3. Claude 会读取 `.mcp.json`（已配置好指向本地 fork）
4. 输入：

```
Use moss to swap 1 MON into USDC on Kuru on the local fork.
```

Claude 会自动调用 `.claude/agents/moss-trader.md` 中定义的流程：
- 获取钱包地址和余额
- 调用四个 Moss 工具
- 检查 Warning 和意图对齐
- 写出 Capability JSON
- 交给钱包签名发送

   7.7 检查结果

```bash
# 查看钱包地址
pnpm --filter @themoss/example-agent-swap wallet -- address

# 查看余额
pnpm --filter @themoss/example-agent-swap wallet -- balance
```

   7.8 重置 Fork

本地 fork 是一次性状态。如果你想重新开始：

```bash
# 停止 fork
pkill anvil

# 重新启动（从最新的主网状态开始）
pnpm --filter @themoss/example-agent-swap fork
```

---

## 第八章 常见问题 FAQ

   Q1：`pnpm build` 报错 "Cannot find module @themoss/core"

 原因 ：你可能跳过了 build 步骤，或者 build 没有完成。

 解决 ：确保在项目根目录执行 `pnpm build`。workspace 包之间依赖 `dist/` 中的生成文件，必须先 build 才能做 typecheck 或运行。

   Q2：运行示例时报错 "MOSS_RPC_URL not set"

 原因 ：没有设置 RPC 环境变量。

 解决 ：Moss 默认使用 `https://rpc.monad.xyz`，通常不需要设置。如果报错，尝试显式设置：

```bash
export MOSS_RPC_URL="https://rpc.monad.xyz"
```

   Q3：simulate 返回 `TRACE_FAILED` Warning

 原因 ：你的 RPC 端点不支持 `debug_traceCall`。

 解决 ：确保使用支持 `debug_traceCall`（含 call/log 证据）、`prestateTracer` diff 和 state overrides 的端点。默认的 `https://rpc.monad.xyz` 满足要求。某些托管 fork 服务可能缺少 geth 风格追踪器——此时需要使用本地 `anvil` fork。

   Q4：`slippage: 50` 是什么意思？

 回答 ：Moss 使用 basis points（基点）表示滑点。1 bps = 0.01%，所以 50 bps = 0.5%。这不是百分比——`50` 不是 50% 滑点。

   Q5：为什么 `amountIn` 是字符串而不是数字？

 回答 ：链上数量可能非常大（超过 JavaScript 的 Number 安全范围），因此 Moss 统一使用 十进制字符串 表示链上量。例如 `"1"` 表示 1 MON（在底层会被转换为 `1000000000000000000` wei）。

   Q6：Agent 能直接签名发送交易吗？

 回答 ： 不能。  Moss 的核心设计原则之一是"签名保持独立"。Moss 构建和验证未签名交易，但从不签名、从不发送。签名和发送由独立的钱包进程完成。这是 Moss 的安全边界。

   Q7：我可以在生产环境中使用 Moss 吗？

 回答 ： 暂时不行。  Moss 目前是未经审计的 Alpha 软件。所有示例都在主网状态上模拟，但不要用真实资金签名发送。等待正式审计后的稳定版本。

   Q8：怎么知道模拟结果真的匹配我的意图？

 回答 ：检查结构化 `outcome` 的每个字段，而不是看 Receipt 文本。例如：

```ts
const outcome = simulation.results[0].receipt.outcome;
// 检查：
// - outcome.operation === "swap"
// - outcome.protocol === "kuru"
// - outcome.sender === 你的地址
// - outcome.tokenIn === NATIVE
// - outcome.tokenOut === USDC_ADDRESS
// - outcome.amountIn === "1"
// - outcome.amountOut > 0
```

文本（text）是给人看的展示， 不是证据 。结构化 Outcome 才是证据。

   Q9：怎么开发自己的协议适配器？

 回答 ：从模板开始：

```bash
cp -R packages/protocols/_template packages/protocols/myprotocol
```

然后按照以下顺序：
1. 重命名包，替换所有 `CHANGEME` 标记
2. 添加有源验证的 ABI 和固定地址
3. 声明 `@Protocol`、类型化 Handles 和 Protocol 依赖
4. 定义 Zod 参数契约、Capabilities、Queries 和纯 Receipts
5. 添加正向和反向类型测试、失败测试、一个线上 happy path
6. 导出 Protocol 并添加到应用组合根

详细指南参见 `docs/protocol-onboarding.md`。

   Q10：怎么参与 Moss 的开源贡献？

 回答 ：参见下一章「下一步」。

---

## 第九章 下一步：参与开源贡献

   9.1 贡献路径地图

```
你是...
│
├── 刚接触 Moss
│   └── 从本教程开始 → 跑通 wrap / swap 示例
│       └── 读 docs/getting-started.md 深化理解
│
├── 想贡献代码
│   ├── 新手 → 找 "good first issue" 或 "difficulty:starter" 标签的 Issue
│   └── 有经验 → 开发新的协议适配器（从 _template 开始）
│
├── 想贡献文档
│   └── 改进文档、补充教程、翻译
│
└── 想构建应用
    └── 基于 MCP Server 构建自己的 AI Agent 应用
```

   9.2 提交第一个 PR

1.  Fork 仓库 ：在 GitHub 上点击 Fork
2.  克隆你的 Fork ：

   ```bash
   git clone https://github.com/你的用户名/moss
   cd moss
   git remote add upstream https://github.com/nishuzumi/moss
   ```

3.  创建分支 ：

   ```bash
   git checkout -b feat/your-feature
   ```

4.  开发和测试 ：

   ```bash
   pnpm build
   pnpm test:offline
   pnpm lint
   ```

5.  提交 PR ：在 GitHub 上创建 Pull Request，遵循 PR 模板中的 checklist

   9.3 代码规范

-  Lint ：使用 Biome（`biome.json` 配置）
-  测试 ：离线测试 `pnpm test:offline`；完整测试 `pnpm test`
-  类型 ：`pnpm typecheck`（必须在 build 之后）
-  行尾 ：强制 LF（`.gitattributes` 配置）

   9.4 推荐阅读

| 资源 | 内容 |
| --- | --- |
| [Getting Started](https://github.com/nishuzumi/moss/blob/main/docs/getting-started.md) | 官方入门指南 |
| [MCP Tools Reference](https://github.com/nishuzumi/moss/blob/main/docs/mcp-tools.md) | 四个工具的完整 API |
| [Protocol Onboarding](https://github.com/nishuzumi/moss/blob/main/docs/protocol-onboarding.md) | 协议适配器开发指南 |
| [Agent Safety Rules](https://github.com/nishuzumi/moss/blob/main/docs/agent-skill.md) | Agent 安全规则 |
| [Architecture Decisions](https://github.com/nishuzumi/moss/tree/main/docs/adr) | 架构决策记录 |
| [Security Model](https://github.com/nishuzumi/moss/blob/main/SECURITY.md) | 安全模型 |
| [Contributing Guide](https://github.com/nishuzumi/moss/blob/main/CONTRIBUTING.md) | 贡献指南 |
| [Agent Swap Example](https://github.com/nishuzumi/moss/blob/main/examples/agent-swap/README.md) | 端到端示例 |

   9.5 社区

-  GitHub Issues ：报告 Bug、提 Feature Request
-  Pull Requests ：提交代码贡献
-  Discussions ：（如果开启）讨论问题和想法





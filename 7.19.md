# 7.19 — Day 5 & Day 6: AgentNFT 合约 + Arena 三合约集成

> Week 2 Day 5-6 | 方向：Dev | 2026-07-19

---

## 一、两日完成清单

### Day 5 — AgentNFT (ERC-721) 基础合约

- [x] AgentNFT 合约编写 — 纯手写 ERC-721，无外部依赖
- [x] Agent 属性设计 — strength / speed / strategy / defense / luck
- [x] Mint 机制 — 伪随机属性生成（范围 40-80）
- [x] 经验值与升级系统 — addExperience + 自动升级
- [x] 授权合约机制 — 只有 Arena 可为 NFT 加经验
- [x] 编译测试通过 — 15410 chars, 32 函数, 9 事件

### Day 6 — Arena 三合约集成联调

- [x] Arena 合约编写 — 集成 ScoreLeaderboard + AgentNFT + BattleEngine
- [x] createBattle/joinBattle 绑定 NFT — 验证持有者
- [x] 对战结果 → NFT 经验值 — 胜 +50 / 败 +10 / 平 +20
- [x] 对战结果 → 排行榜里程碑 — Battle Win / Draw / Streak
- [x] 编译测试通过 — 19806 chars, 26 函数, 7 事件
- [x] README 编写 — 完整 Remix 部署流程
- [x] GitHub 提交内容整理 — 3 合约 + README + .gitignore

---

## 二、AgentNFT 合约设计

### 2.1 为什么纯手写 ERC-721

| 方案 | 优点 | 缺点 |
|------|------|------|
| OpenZeppelin | 功能完整，安全审计 | 需 npm install，Remix 不方便 |
| 纯手写 | 零依赖，直接粘贴到 Remix | 需自己实现所有标准函数 |

选择纯手写：部署方式是 Remix + MetaMask，不装任何依赖。代码完全自包含。

### 2.2 Agent 属性体系

```
┌──────────────────────────────────────────┐
│            Agent NFT 属性体系             │
├──────────┬───────────────────────────────┤
│ strength │ 攻击力 — 影响攻击伤害          │
│ speed    │ 速度   — 影响出手顺序 / 闪避   │
│ strategy │ 策略   — 影响暴击率            │
│ defense  │ 防御   — 减少受到的伤害        │
│ luck     │ 运气   — 随机增益             │
└──────────┴───────────────────────────────┘
```

| 设计决策 | 理由 |
|----------|------|
| 属性范围 40-80 | 保证初始不会太强也不会太弱，有成长空间 |
| 5 项属性 | 足够丰富，不会 Stack too deep |
| 上限 100 | 升级有目标，避免无限增长 |
| 每次升级 +1~+3 | 成长感明显但不会太快满级 |

### 2.3 经验值与升级公式

```
Level 1 → 2:  需要 100 XP
Level 2 → 3:  需要 200 XP
Level 3 → 4:  需要 300 XP
...
Level N → N+1: 需要 N × 100 XP

对战经验值:
  胜利 = +50 XP  (2 场胜利可升 Level 2)
  失败 = +10 XP  (参与奖，10 场败可升级)
  平局 = +20 XP  (5 场平局可升级)
```

### 2.4 Mint 属性生成

```solidity
function _randomStat(uint256 seed, uint256 offset) internal pure returns (uint256) {
    uint256 rand = uint256(keccak256(abi.encodePacked(seed, offset)));
    return 40 + (rand % 41); // 40 + (0~40) = 40~80
}
```

**伪随机数种子：**
```solidity
seed = keccak256(blockhash + minter + tokenId + timestamp + gasleft)
```

**已知限制：** blockhash 在某些环境返回 0，且矿工可操控。生产环境应使用 Chainlink VRF。

### 2.5 AgentNFT 函数清单

**ERC-721 标准：**
- `balanceOf` / `ownerOf` / `transferFrom` / `safeTransferFrom`
- `approve` / `setApprovalForAll` / `getApproved` / `isApprovedForAll`
- `tokenURI` / `totalSupply` / `tokenByIndex` / `tokenOfOwnerByIndex`

**Agent 专用：**
- `mintAgent(uri)` — 铸造新 Agent
- `getAgent(tokenId)` — 返回 11 个字段完整属性
- `getTotalPower(tokenId)` — 5 属性之和
- `getWinRate(tokenId)` — 胜率（万分比）
- `getLevel(tokenId)` — 当前等级
- `addExperience(tokenId, exp)` — 加经验（仅授权合约）
- `recordBattle(tokenId, won)` — 记录对战（仅授权合约）

**管理：**
- `addAuthorizedContract` / `removeAuthorizedContract` — 授权管理
- `setMintPrice` / `withdraw` — 经济管理

---

## 三、Arena 三合约集成设计

### 3.1 与原版 BattleEngine 的区别

| 对比项 | BattleEngine (Day 2) | Arena (Day 5-6) |
|--------|---------------------|------------------|
| 依赖合约 | ScoreLeaderboard | ScoreLeaderboard + AgentNFT |
| createBattle | `createBattle(maxRounds)` | `createBattle(maxRounds, nftId)` |
| joinBattle | `joinBattle(battleId)` | `joinBattle(battleId, nftId)` |
| NFT 验证 | 无 | 验证调用者持有 NFT |
| 对战结果 | 排行榜里程碑 | 排行榜里程碑 + NFT 经验值 |
| Battle 结构体 | 10 字段 | 12 字段（+nftId1, nftId2） |
| 事件参数 | 7 字段 | BattleEnded 多 winnerNftId, loserNftId |

### 3.2 NFT 经验值分配

```
对战结束 → _updateNFTs(battleId)

if (平局):
  agentNFT.recordBattle(nftId1, false)  // 记录败（平局不算赢）
  agentNFT.recordBattle(nftId2, false)
  agentNFT.addExperience(nftId1, 20)   // 各 +20 XP
  agentNFT.addExperience(nftId2, 20)

if (有胜者):
  winnerNft = 胜者的 NFT ID
  loserNft  = 败者的 NFT ID
  agentNFT.recordBattle(winnerNft, true)   // 记录胜
  agentNFT.recordBattle(loserNft, false)   // 记录败
  agentNFT.addExperience(winnerNft, 50)    // 胜者 +50 XP
  agentNFT.addExperience(loserNft, 10)     // 败者 +10 XP
```

### 3.3 为什么用 try-catch

Arena 调用 AgentNFT 的 `addExperience` 和 `recordBattle` 时使用 `try-catch`：
- 如果 AgentNFT 合约出问题（如 paused），不会影响对战本身的结算流程
- 对战奖金分配和排行榜更新必须在 NFT 经验值更新失败时仍然执行

### 3.4 Arena 新增功能

**`getBattleNFTLevels(battleId)`** — 查询双方 NFT 等级
```
返回 (level1, level2)
方便前端在对战页面显示双方 Agent 等级
```

### 3.5 四个里程碑任务

| 任务 | 分值 | 触发条件 | 变量 |
|------|------|----------|------|
| Battle Win | 5 | 首次赢得对战 | taskBattleWin |
| Battle Draw | 2 | 首次平局 | taskBattleDraw |
| Win Streak x3 | 10 | 首次 3 连胜 | taskWinStreak3 |
| First Agent Mint | 3 | 首次铸造 Agent NFT | taskFirstAgentMint |

---

## 四、编译信息

### AgentNFT

| 项目 | 值 |
|------|-----|
| 编译器 | solc 0.8.28 (viaIR) |
| Bytecode | 15410 chars |
| ABI 函数 | 32 |
| ABI 事件 | 9 |
| 优化器 | enabled, runs=200 |

### Arena

| 项目 | 值 |
|------|-----|
| 编译器 | solc 0.8.28 (viaIR) |
| Bytecode | 19806 chars |
| ABI 函数 | 26 |
| ABI 事件 | 7 |
| 优化器 | enabled, runs=200 |

---

## 五、部署顺序

```
1. 部署 ScoreLeaderboard
   → 构造函数无参数
   → 记录地址 ADDR_LEADERBOARD

2. 部署 AgentNFT
   → 构造函数: mintPrice_ = 0 (免费)
   → 记录地址 ADDR_NFT

3. 部署 Arena
   → 构造函数: leaderboard_ = ADDR_LEADERBOARD
               agentNFT_    = ADDR_NFT
   → 记录地址 ADDR_ARENA

4. Arena.initialize()
   → 注册 4 个里程碑任务
   → Arena 成为排行榜验证者

5. AgentNFT.addAuthorizedContract(ADDR_ARENA)
   → 授权 Arena 为 NFT 加经验

6. 开始使用:
   mintAgent → createBattle → joinBattle → submitMove × N
```

---

## 六、完整交互演示

### 模拟一场 Best of 3 对战

```
准备:
  账户 A: mintAgent("") → tokenId = 1, 属性: str=65 spd=52 stg=71 def=48 lck=60
  账户 B: mintAgent("") → tokenId = 2, 属性: str=55 spd=68 stg=45 def=72 lck=50

对战:
  A: createBattle(3, 1)                → battleId = 0, state = Waiting
  B: joinBattle(0, 2)                  → state = InProgress

  第 1 轮:
  A: submitMove(0, Rock)               → MoveExecuted
  B: submitMove(0, Scissors)            → MoveExecuted → RoundResolved
  结果: Rock beats Scissors → P1 赢 → p1Score = 1

  第 2 轮:
  A: submitMove(0, Paper)              → MoveExecuted
  B: submitMove(0, Rock)               → MoveExecuted → RoundResolved
  结果: Paper beats Rock → P1 赢 → p1Score = 2

  p1Score(2) >= winsNeeded(2) → 对战结束!

结算:
  winner = A, loser = B, isDraw = false, p1Score=2, p2Score=0

  NFT 更新:
    NFT #1: +50 XP, totalBattles=1, wins=1
    NFT #2: +10 XP, totalBattles=1, wins=0

  排行榜:
    A: +5 分 (Battle Win 里程碑)
    B: 0 分

查询:
  Arena.getBattle(0):
    player1=A, player2=B, nftId1=1, nftId2=2
    state=Finished, winner=A, p1Score=2, p2Score=0

  AgentNFT.getAgent(1):
    strength=65, speed=52, ..., level=1, experience=50, expToNext=100

  AgentNFT.getAgent(2):
    strength=55, speed=68, ..., level=1, experience=10, expToNext=100

  ScoreLeaderboard.getScore(A) → 5
  ScoreLeaderboard.getRank(A)  → 1
```

---

## 七、安全考量

| 机制 | 说明 |
|------|------|
| NFT 持有验证 | createBattle/joinBattle 验证调用者持有 NFT |
| 同一 NFT 不能双方使用 | `require(nftId != b.nftId1)` |
| 授权合约机制 | 只有 addAuthorizedContract 授权的地址可加经验 |
| try-catch 容错 | NFT 更新失败不影响对战结算 |
| viaIR 编译 | 解决 getBattle 12 个返回值的 Stack too deep |
| 防重复提交 | p1Submitted / p2Submitted 标记 |
| 状态校验 | 每个函数校验 BattleState |
| 入场费校验 | joinBattle 校验 stake 一致 |
| 奖金分配 | 胜者通吃 / 平局退还，链上自动 |

---

## 八、错误记录

| # | 错误 | 原因 | 修复 | 耗时 |
|---|------|------|------|------|
| 1 | Arena.sol ParserError | `try...catch {}` 后多了 `;` | 删除多余分号 | 5min |
| 2 | Stack too deep (Arena) | getBattle 返回 12 个值 | 开启 viaIR | 已知问题 |

---

## 九、AI 协作记录

| 任务 | AI 贡献 | 人工贡献 |
|------|---------|---------|
| AgentNFT 架构 | 建议纯手写 ERC-721 方案 | 确认无依赖适合 Remix |
| 属性设计 | 生成 5 属性体系 | 确定属性范围 40-80 |
| 升级公式 | 建议 Level N → N+1 需 N*100 XP | 确认 XP 数值 50/10/20 |
| Arena 集成 | 生成 try-catch 容错逻辑 | 确认授权机制设计 |
| 编译修复 | — | 手动修复 catch 后多余分号 |
| README | 生成完整部署流程 | 审查确认步骤可操作 |

---

## 十、GitHub 提交内容

```
github-ready-v2/
├── ScoreLeaderboard.sol       # 合约 1: 任务系统 + 排行榜 (Week 1)
├── AgentNFT.sol               # 合约 2: ERC-721 Agent NFT (Day 5)
├── Arena.sol                  # 合约 3: 三合约集成对战引擎 (Day 6)
├── README.md                  # 完整说明 + Remix 部署流程
├── .gitignore                 # 排除 .env / node_modules
└── ScoreLeaderboard-README.md # Week 1 README (参考)
```

---

## 十一、下一步计划

- [ ] Day 7: 在 Remix 中部署三个合约到 Monad Testnet
- [ ] Day 7: 完成至少 1 场完整对战交互测试
- [ ] Day 7: 截图记录部署和对战过程
- [ ] Day 7: 周末回顾 + 更新 Role Log
- [ ] Week 3: 实现 commit-reveal 防前跑
- [ ] Week 3: NFT 属性影响战斗判定
- [ ] Week 3: DApp 前端

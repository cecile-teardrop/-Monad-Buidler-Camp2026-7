# Week 1 Build Log — Monad Buidler Camp

> 本文档记录 Week 1 的链上实践、问题修复、AI 协作过程、认知变化及 Week 2 方向选择。



  一、链上实践记录

 # 1.1 钱包与网络配置

| 项目 | 值 |
|------|-----|
| 钱包 | MetaMask |
| 网络 | Monad Testnet |
| Chain ID | `10143` |
| RPC URL | `https://testnet-rpc.monad.xyz` |
| 货币符号 | `MON` |
| 区块浏览器 | `https://testnet.monadscan.com` |
| 水龙头 | `https://faucet.monad.xyz` |
| 部署者地址 | `0x6A804B989AAfb5206BBDbE708CB31Ee60b7297Dd` |

 # 1.2 合约部署与交互记录

 合约：SimpleStorage  — 一个极简链上存储合约，包含 read / write 功能。

| 项目 | 值 |
|------|-----|
| 合约地址 | `0x864eD8d14e58d77d081C1CDC54478CB32019954e` |
| 部署交易 Hash | `0xdf09964ca94f35ebae190216b2a683f20269e6943cb77152c2e5d19f49f4160d` |
| 交互交易 Hash (set) | `0xfc88e7a6181999abc6b535acfe76989f7081f2ad3346656b12420a96e4ae2486` |
| 部署区块 | `43682463` |
| 部署时间 | 2026-07-10 |
| 部署工具 | Remix IDE + MetaMask (Injected Provider) |

 交互流程： 


1. Read 调用 get()        → 返回 0（初始值）
2. Read 调用 getOwner()   → 返回部署者地址
3. Write 调用 set(42)     → MetaMask 签名 → 交易上链 → status: 1 (成功)
4. Read 调用 get()        → 返回 42（验证 write 生效）


 区块浏览器验证链接： 
- 合约地址：[查看合约](https://testnet.monadscan.com/address/0x864eD8d14e58d77d081C1CDC54478CB32019954e)
- 部署交易：[查看 Tx](https://testnet.monadscan.com/tx/0xdf09964ca94f35ebae190216b2a683f20269e6943cb77152c2e5d19f49f4160d)
- 交互交易：[查看 Tx](https://testnet.monadscan.com/tx/0xfc88e7a6181999abc6b535acfe76989f7081f2ad3346656b12420a96e4ae2486)

 # 1.3 合约源码

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

contract SimpleStorage {
    uint256 private storedValue;
    address public owner;

    event ValueChanged(uint256 oldValue, uint256 newValue, address changedBy);

    constructor() {
        owner = msg.sender;
        storedValue = 0;
    }

    function set(uint256 _value) public {
        uint256 oldValue = storedValue;
        storedValue = _value;
        emit ValueChanged(oldValue, _value, msg.sender);
    }

    function get() public view returns (uint256) {
        return storedValue;
    }

    function getOwner() public view returns (address) {
        return owner;
    }
}




  二、问题与修复记录

 # 问题 1：脚本部署 vs Remix 部署的选择

 背景：  AI 生成了完整的命令行部署脚本（deploy.js），需要在 `.env` 中填写 `PRIVATE_KEY`。

 问题：  将私钥放入项目 `.env` 文件存在安全风险，即使 `.gitignore` 已排除，本地开发环境仍可能泄露。

 修复过程： 
1. AI 提供了脚本方案，但涉及私钥管理
2. 人工判断：改用 Remix IDE 的 Injected Provider - MetaMask 方式部署
3. MetaMask 直接签名，私钥不离开钱包，更安全
4. 最终通过 Remix 完成部署和交互

 教训：  工具选择不只是效率问题，更是安全问题。AI 能给出"可行"的方案，但"安全"需要人工判断。



 # 问题 2：区块浏览器 URL 不一致

 背景：  AI 初始提供的区块浏览器 URL 为 `https://testnet.monadvision.com`。

 问题：  实际访问时发现该域名已变更或不可用，正确的区块浏览器为 `https://testnet.monadscan.com`。

 修复过程： 
1. 访问 Monad 官方文档确认正确 URL
2. 修正 `deploy-info.json`、README 和脚本中的所有引用
3. 更新为 `https://testnet.monadscan.com`

 教训：  AI 的知识可能存在时效性问题，链上工具的 URL 必须人工核实。



 # 问题 3：Read 与 Write 的概念理解

 背景：  初次接触合约交互时，对 read / write 的区别理解不够直观。

 问题：  不清楚为什么 `get()` 不需要 MetaMask 签名而 `set()` 需要。

 修复过程： 
1. AI 解释：read 调用是 view 函数，只读取链上状态，不上链、不花 gas
2. write 调用修改链上状态，需要全网共识，因此需要签名 + 付费
3. 通过实际操作验证：`get()` 秒回结果，`set()` 弹出 MetaMask 确认

 教训：  概念理解最终要通过操作来巩固——"做一遍"比"看十遍"有效。



  三、AI 协作记录

 # 3.1 AI 帮我完成了什么

| 任务 | AI 贡献 | 人工贡献 |
|------|---------|---------|
| 合约编写 | 生成 SimpleStorage.sol 完整源码 | 审查逻辑正确性、选择部署方式 |
| 工程脚本 | 编写 compile/deploy/interact 三件套 | 决定不使用脚本部署（安全考量） |
| 部署文档 | 生成 README 和 deploy-info.json 模板 | 填入实际部署数据、修正浏览器 URL |
| 高频交互分析 | 按 9 个方向 × 4 个问题生成结构化笔记 | 确认分析方向、补充个人理解 |
| 应用方案 | 撰写 AI Agent 竞技场完整方案（Research/Tech/Ops） | 选择方向、评估可行性 |
| AI Agent 安全 | 将原始思考整理为结构化安全笔记 | 提供核心观点和框架思路 |

 # 3.2 Prompt → AI 输出 → 人工修改示例

 Prompt（简化）： 
> "Monad 高频交互有哪些方向？每个方向为什么需要链上？"

 AI 输出： 
- 9 个方向的结构化分析（高频交互、低延迟体验、Consumer Crypto、游戏、Onchain Social、AI Agent、排行榜、实时反馈、高频交易）
- 每个方向回答 4 个问题

 人工修改： 
- 确认 9 个方向覆盖全面，无需增减
- 补充个人理解：强调"可验证的公平竞争"是核心价值
- 选择 AI Agent 竞技场作为最终方案方向
- 修正区块浏览器 URL（monadvision → monadscan）

 # 3.3 哪些地方必须由人人工判断

| 判断点 | 为什么 AI 不能替代 |
|--------|-------------------|
| 部署方式选择 | AI 给出脚本方案"可行"，但安全性需要人评估——私钥管理是安全决策 |
| 方向选择 | AI 能列出 9 个方向，但"选哪个深入"取决于个人兴趣和技能匹配 |
| 安全 URL 核实 | AI 的知识有时效性，链上工具 URL 必须人工验证 |
| 交互结果验证 | AI 说"部署成功"不够，必须自己在区块浏览器上看到 `status: 1` |
| 方案可行性判断 | AI 写的伪代码"看起来对"，但是否真正可实现需要人评估 |
| 价值观判断 | "链上 vs 数据库"不只是技术问题，是关于信任、自主权的价值选择 |



  四、对 Monad / Web3 的理解变化

 # 4.1 Before → After

| 维度 | Before（Week 1 前） | After（Week 1 后） |
|------|---------------------|---------------------|
| 区块链速度 | "区块链就是慢" | Monad 的 400ms 出块 + 10K TPS 让链上实时交互变得可行 |
| Gas 成本 | "每次操作都花钱" | Monad 低 Gas 让微交互（0.001 美元级）经济可行 |
| 链上 vs 链下 | "为什么不把逻辑放链下？" | 传统链被迫移到链下是因为 TPS 不够；Monad 改变了这个等式 |
| 信任最小化 | "区块链就是去中心化" | 核心价值是"可验证"——任何人都能审计，不需要信任任何中间人 |
| EVM 兼容 | 没有概念 | Solidity / Foundry / MetaMask 零迁移成本，是 Monad 的关键优势 |

 # 4.2 最重要的认知转变

 传统链的困境： 

高频交互需求  ←→  低 TPS + 高 Gas + 慢出块  =  体验崩塌
→ 被迫将核心逻辑移到链下 → 失去"可验证"的核心价值

 Monad 的范式转变： 

10,000 TPS + 400ms 出块 + 低 Gas + EVM 兼容
→ 链上实时交互变得可行 → "可验证的公平竞争"不再是妥协产物

 一句话：  之前认为区块链的"慢和贵"是固有属性，现在理解到这是特定技术架构的限制——Monad 通过并行执行 + 流水线共识打破了这一限制，让链上高频交互从"理论可行"变成"用户体验可行"。

 # 4.3 对 AI Agent × Web3 的新理解

在撰写 AI Agent 安全思考和竞技场方案的过程中，形成了以下认知：

- AI Agent 的决策链是多步的、连续的，天然需要高频链上交互
- 链上是多方 Agent 可信协调的唯一层——否则无法验证对方行为是否合规
- AI Agent 安全的核心是权限控制：低权限是地基，二次确认是阀门，安全软件是雷达
- "用计算机预防计算机"——面对无法人工察觉的攻击，自动化防御是唯一务实策略


  五、方向选择：

初步选择：Tech（技术方向）

 理由：

1.  已有实践基础：  Week 1 已完成合约编写、编译、部署、交互的完整链路，对 Solidity 语法和 ethers.js 工具链有实际操作经验。

2.  技能匹配：  本身有 Python / 编程基础，对代码逻辑、状态管理、事件机制理解较快。

3.  方案已具技术雏形：  AI Agent 竞技场方案中已设计 5 个核心合约（AgentNFT / ArenaRegistry / BattleEngine / Leaderboard / RewardPool），Week 2 可以从伪代码推进到实际实现。

4.  兴趣驱动：  对"让东西跑起来"比"写分析报告"更有热情——亲手部署合约并在区块浏览器上看到 `status: 1` 的成就感是驱动力。

5.  Research 作为辅助：  Week 1 的高频交互分析和 AI Agent 安全思考表明具备研究能力，但更希望将研究转化为可运行的产品。

 Week 2 目标： 
- 将 BattleEngine 核心合约从伪代码推进到可编译、可部署的 Solidity 实现
- 实现基本的 Agent 铸造 + 提交动作 + 状态更新逻辑
- 在 Monad Testnet 上部署并完成一次完整的对战交互

  六、本周最重要的 3 个学习收获

 # 收获 1：理解了 Read / Write 的本质区别

`get()` 是 view 函数，只读取链上状态，不上链、不花 gas、秒回结果；`set()` 修改链上状态，需要全网共识，必须签名 + 付费 + 等待确认。这不是语法差异，而是"查询"与"状态变更"的本质区别——链上状态变更是有成本的。

 # 收获 2：认识到工具选择是安全决策

AI 给出的脚本部署方案"可行"但不是"最优"——将私钥放入 `.env` 有泄露风险。改用 Remix + MetaMask 的 Injected Provider 方式，私钥不离开钱包，更安全。 AI 能给出方案，但安全性和工具选择需要人判断。 

 # 收获 3：理解了"为什么需要链上"的真正理由

不是所有数据都需要上链。纯个人状态用数据库足够；但涉及 多方共识 （竞拍、投票、资产转移）和 可验证公平 （竞技对战、排行榜）时，链上不可替代——因为链上执行意味着任何人都能审计，无需信任任何中间人。Monad 的价值是让这个"不可替代"在经济和体验上都变得可行。

---

  七、希望助教或同伴帮助的问题

 # 问题 1：Monad 并行执行对合约开发的影响
Monad 的并行执行模型是否意味着合约编写需要特别注意状态依赖？如果两个 `submitMove` 同时执行，是否需要特殊的并发控制模式？如何设计合约才能最大化利用并行执行的优势？

 # 问题 2：Foundry / Hardhat 工具链选择
Week 1 使用了 Remix + solc-js 的组合。Week 2 如果要开发更复杂的多合约系统（5 个合约），应该选择 Foundry 还是 Hardhat？各自的优劣势？Monad Testnet 的配置是否有坑？

 # 问题 3：链上事件 → 前端实时渲染的最佳实践
BattleEngine 的 `MoveExecuted` event 需要实时推送到前端渲染对战画面。在 Monad 的 400ms 出块频率下，直接监听 RPC event 是否足够？还是需要 The Graph 等索引层？有没有更轻量的方案？

 # 问题 4：AI Agent 链下推理与链上提交的架构设计
Agent 的 AI 决策在链下完成（LLM 推理），仅将动作结果提交链上。这个"链下推理 → 链上执行"的架构中，如何确保 Agent 的决策逻辑可验证？是否需要将决策过程也上链（如 commit-reveal 模式）？


  八、Week 1 产出物清单

| 产出物 | 类型 | 文件 |
|--------|------|------|
| SimpleStorage 合约 | Solidity | `monad-mini-contract/contracts/SimpleStorage.sol` |
| 编译/部署/交互脚本 | JavaScript | `monad-mini-contract/scripts/*.js` |
| 部署记录 | JSON | `monad-mini-contract/deploy-info.json` |
| 合约部署文档 | Markdown | `monad-mini-contract/第一个 Monad 合约.md` |
| 高频交互笔记（9方向） | Markdown | `Monad高频交互笔记.md` |
| 高频交互应用方案 | Markdown + HTML | `Monad高频交互应用方案.md` / `.html` |
| 高频交互总结矩阵 | HTML | `Monad高频交互总结矩阵.html` |
| AI Agent 安全思考 | Markdown | `AI_Agent安全思考.md` |


  九、Week 2 展望

Week 1 完成了：
  ✅ 最小合约部署 + 交互（SimpleStorage）
  ✅ Monad 高频交互方向分析（9 方向）
  ✅ 应用方案设计（AI Agent 竞技场）
  ✅ AI Agent 安全思考

Week 2 目标：
  ⬜ BattleEngine 核心合约实现（从伪代码到可部署）
  ⬜ AgentNFT (ERC-721) 铸造功能
  ⬜ 基本对战循环：submitMove → 状态更新 → 胜负判定
  ⬜ Monad Testnet 部署 + 完整对战交互
  ⬜ 前端对战画面渲染（如果时间允许）
>  一句话总结 Week 1：  从零到部署第一个链上合约，理解了"为什么需要链上"和"Monad 为什么不同"，选择了 Tech 方向，准备好在 Week 2 把 AI Agent 竞技场从设计推向实现。
